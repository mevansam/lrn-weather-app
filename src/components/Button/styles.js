import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
  },
  button: {
    flex: 1,
    flexDirection: "row"
  },
  text: {
    fontWeight: "bold",
    textAlign: "center"
  }
});
