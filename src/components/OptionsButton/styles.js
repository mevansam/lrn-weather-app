import { StyleSheet } from "react-native";

import { colors } from './../../styles/common';

const height = 26
export const iconColor = colors.lightGray

export default StyleSheet.create({
  container: {
    height: height
  },
  button: {
  },
  text: {
  }
});
